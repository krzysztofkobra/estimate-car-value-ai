# estimate-car-value-ai (FastAPI ML service) — copy to .env and fill in.
# Placeholders only; NO real values committed here.

# Port the API listens on (api.py defaults to 8000 if unset).
PORT=8000

# --- Training/data DB (only needed to run fetch_data.py / car_price_model.py;
#     the serving API api.py does NOT use these) ---
DBNAME=
USER=
PASSWORD=
HOST=
# Note: training scripts also read PORT (above) as the DB port.
